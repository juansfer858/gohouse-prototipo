name: Probe QR production

on:
  push:
    branches: [gohouse-vps-recovery]
    paths:
      - '.github/workflows/probe-qr.yml'

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Verify rendered QR button
        run: |
          set -euo pipefail
          BROWSER=""
          for c in google-chrome google-chrome-stable chromium chromium-browser; do
            if command -v "$c" >/dev/null 2>&1; then BROWSER="$c"; break; fi
          done
          test -n "$BROWSER"
          timeout 30s "$BROWSER" --headless --disable-gpu --no-sandbox --virtual-time-budget=8000 --dump-dom \
            https://domicilios-llanos.vantixgc.com/panel/ >/tmp/panel-dom.html 2>/tmp/browser.err || true
          grep -n 'id="tab-qr-public"' /tmp/panel-dom.html
          grep -n 'QR de pedidos' /tmp/panel-dom.html | head -5
          echo 'RENDERED_QR_BUTTON_OK=true'
